<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StreetBites</title>
    
    <link rel="stylesheet" href="/style.css" />
    <link rel="stylesheet" href="/stylesextra.css" />

    <!-- GSAP CDN -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/SplitText.min.js"></script>

    <style>
        /* Full-screen background */
        body {
            background: url("https://img.freepik.com/free-photo/top-view-tasty-vegetable-rolls-whole-sliced-with-greens-seasonings-bright-blue-desk-pepper-meal-roll-vegetable_140725-31034.jpg?ga=GA1.1.105797660.1738641078&semt=ais_hybrid") 
                        no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            width: 100vw;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .container-body {
            margin: 3% 10%;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            flex-direction: column;
        }

        #quote {
            font-size: clamp(2rem, 10rem, 5vw);
            color: white;
            text-align: center;
            line-height: 1.2;
            font-weight: bold;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.7);
        }

        button {
            padding: 10px 20px;
            margin: 20px 0;
            background-color: #ff4500;
            color: white;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            background-color: #e63900;
        }
    </style>
</head>

<body>
    <!-- Header -->
    <header class="bg-white bg-opacity-80 text-black p-4 shadow-md w-full fixed top-0">
      <nav class="container mx-auto flex justify-between items-center">
        <ul class="flex space-x-4">
          <li>
            <a href="/first" class="text-3xl font-bold px-4">StreetBites</a>
          </li>
          <li>
            <a href="/index" class="font-bold py-2 px-4 rounded transition duration-300 hover:bg-red-700 text-white">Restaurants-List</a>
          </li>
          <li>
            <a href="/favourites" class="py-2 px-4 font-bold rounded transition duration-300 hover:bg-red-700 text-white">Favourites</a>
          </li>
          <li>
            <a href="/bucket" class="py-2 px-4 font-bold rounded transition duration-300 hover:bg-red-700 text-white">Bucket</a>
          </li>
          <li>
            <a href="/host/host-resturant-list" class="py-2 px-4 font-bold rounded transition duration-300 hover:bg-red-700 text-white">Host Restaurants</a>
          </li>
          <li>
            <a href="/host/add-resturant" class="py-2 px-4 font-bold rounded transition duration-300 hover:bg-red-700 text-white">Add Restaurants</a>
          </li>
        </ul>
      </nav>
    </header>

    <!-- Quote & Button Section -->
    <div class="container-body">
      <div id="quote">
        "SplitText makes it easy to break apart text in an HTML element so that each character, word, and/or line is wrapped in its own div tag."
      </div>
      <button id="animate">Animate</button>
    </div>

    <!-- GSAP Animation Script -->
    <script>
      gsap.registerPlugin(SplitText);

      var tl = gsap.timeline(),
          mySplitText = new SplitText("#quote", { type: "words,chars" }),
          chars = mySplitText.chars; // Array of all character divs

      gsap.set("#quote", { perspective: 400 });

      tl.from(chars, {
          duration: 0.8,
          opacity: 0,
          scale: 0,
          y: 80,
          rotationX: 180,
          transformOrigin: "0% 50% -50",
          ease: "back",
          stagger: 0.01
      });

      document.getElementById("animate").onclick = function () {
          tl.restart();
      };
    </script>
</body>
</html>
